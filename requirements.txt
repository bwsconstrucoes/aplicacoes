Flask
requests
dropbox
PyMuPDF
PyPDF2
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib
gunicorn
pdfplumber==0.11.4
gspread==6.1.4
pypdf==5.1.0
openai
signxml
cryptography
lxml
fpdf2
num2words
qrcode[pil]
SQLAlchemy>=2.0
psycopg2-binary
pillow
# Código de barras do boleto (Análise de SPs). Python puro, sem nada
# compilado — a tela que a usa vem numa entrega seguinte.
python-barcode>=0.15
openpyxl  # relatorio do painel em Excel (uma aba por assunto)

# DEPENDENCIA NOVA, 26/09/2026 - avisada ao dono no mesmo dia.
#
# A Folha Sintetica que a contabilidade manda e gerada pelo Fortes Pessoal no
# formato BIFF do Excel 97 (.xls). O openpyxl NAO abre .xls, so .xlsx - e pedir
# para a contabilidade externa trocar o formato do relatorio nao e uma opcao que
# esteja na nossa mao. O xlrd e a biblioteca padrao para isso, so faz leitura, e
# nao tem dependencia nenhuma alem da propria.
xlrd

# Busca automatica de notas na Receita (NF-e), para o Analise de SPs.
# CODIGO DE TERCEIRO DE PROPOSITO: ela resolve a assinatura digital do pedido
# com o certificado A1, que e a parte onde escrever do zero custa caro - o erro
# volta como "recusado" sem dizer por que. Autorizada pelo dono em 12/09/2026.
erpbrasil.edoc==3.1.1
erpbrasil.assinatura==1.8.0
